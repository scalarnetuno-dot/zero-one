# Toda a pipeline roda com isto. Nenhuma instalação de sistema:
# o compilador Typst vem dentro do pacote `typst`.
typst>=0.13        # motor de composição (PDF)
Jinja2>=3.1        # templates do design system (.typ e .css)
PyYAML>=6.0        # tokens, metadados e specs de diagrama
Pygments>=2.17     # realce de sintaxe no EPUB
pypdf>=4.0         # validação do PDF pronto e contagem de páginas
Pillow>=10.0       # checagem de DPI das imagens
